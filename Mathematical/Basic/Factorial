Problem Statement:
Given a positive integer, N. Find the factorial of N.

Solution:
static long factorial(int N)
    {
        long result=N;
        if(N==0)
            return 1;
        for(int i=N-1;i>=1;--i)
            result*=i;
        return result;
    }
